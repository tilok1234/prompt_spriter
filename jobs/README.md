# Sprite job templates

This directory contains committed instructions and examples, not production
sprite output. Live work belongs under the ignored `workspace/staging/` root.

Templates make paths and contracts predictable while leaving the user's
Antigravity request conversational.
